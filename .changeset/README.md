# Changesets

This directory stores pending release notes for Changesets.

To create a release note for a user-facing change, run:

```bash
pnpm changeset
```

Merging the generated release PR publishes the package to npm from GitHub Actions.
